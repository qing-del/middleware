<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.jacolp.module.audit.biz.infrastructure.persistence.mapper.ImageAuditMapper">

    <!-- resultMap：将数据库字段映射到 VO -->
    <resultMap id="ImageAuditVO" type="com.jacolp.module.audit.biz.application.vo.ImageAuditVO">
        <id column="id" property="id"/>
        <result column="applicant_user_id" property="applicantUserId"/>
        <result column="applicant_username" property="applicantUsername"/>
        <result column="image_id" property="imageId"/>
        <result column="filename" property="filename"/>
        <result column="oss_url" property="ossUrl"/>
        <result column="apply_reason" property="applyReason"/>
        <result column="status" property="status"/>
        <result column="reviewer_user_id" property="reviewerUserId"/>
        <result column="reviewer_username" property="reviewerUsername"/>
        <result column="reject_reason" property="rejectReason"/>
        <result column="create_time" property="createTime"/>
        <result column="review_time" property="reviewTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <resultMap id="ImageAuditRecordMap" type="com.jacolp.module.audit.biz.infrastructure.persistence.dataobject.ImageAuditRecordDO">
        <id column="id" property="id"/>
        <result column="applicant_user_id" property="applicantUserId"/>
        <result column="image_id" property="imageId"/>
        <result column="apply_reason" property="applyReason"/>
        <result column="status" property="status"/>
        <result column="reviewer_user_id" property="reviewerUserId"/>
        <result column="reject_reason" property="rejectReason"/>
        <result column="create_time" property="createTime"/>
        <result column="review_time" property="reviewTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <!-- 新增审核记录 -->
    <insert id="insertAuditRecord" parameterType="com.jacolp.module.audit.biz.infrastructure.persistence.dataobject.ImageAuditRecordDO" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO biz_image_audit_record (applicant_user_id, image_id, apply_reason, status, create_time)
        VALUES (#{applicantUserId}, #{imageId}, #{applyReason}, 1, NOW())
    </insert>

    <!-- 修改审核记录（批准或拒绝） -->
    <update id="updateAuditRecord" parameterType="com.jacolp.module.audit.biz.infrastructure.persistence.dataobject.ImageAuditRecordDO">
        UPDATE biz_image_audit_record
        <set>
            <if test="status != null">
                status = #{status},
            </if>
            <if test="reviewerUserId != null">
                reviewer_user_id = #{reviewerUserId},
            </if>
            <if test="rejectReason != null and rejectReason != ''">
                reject_reason = #{rejectReason},
            </if>
            review_time = NOW()
        </set>
        WHERE id = #{id} AND status = 1
    </update>

    <!-- 条件查询审核列表（管理员用）-->
    <select id="listByCondition" resultMap="ImageAuditVO">
        SELECT
            a.id,
            a.applicant_user_id,
            applicant.username AS applicant_username,
            a.image_id,
            subject.target_name AS filename,
            subject.target_url AS oss_url,
            a.apply_reason,
            a.status,
            a.reviewer_user_id,
            reviewer.username AS reviewer_username,
            a.reject_reason,
            a.create_time,
            a.review_time,
            a.update_time
        FROM biz_image_audit_record a
        LEFT JOIN audit_query_user_projection applicant ON applicant.user_id = a.applicant_user_id
        LEFT JOIN audit_query_user_projection reviewer ON reviewer.user_id = a.reviewer_user_id
        LEFT JOIN audit_query_subject_projection subject
               ON subject.target_type = 'IMAGE' AND subject.target_id = a.image_id
        <where>
            <if test="status != null">a.status = #{status}</if>
            <if test="applicantUserId != null">AND a.applicant_user_id = #{applicantUserId}</if>
        </where>
        ORDER BY a.update_time DESC, a.id DESC
    </select>

    <!-- 查询待审核审核列表 -->
    <select id="listPendingAudits" resultMap="ImageAuditVO">
        SELECT
            a.id,
            a.applicant_user_id,
            a.image_id,
            a.apply_reason,
            a.status,
            a.reviewer_user_id,
            a.reject_reason,
            a.create_time,
            a.review_time,
            a.update_time
        FROM biz_image_audit_record a
        WHERE a.status = 1
        ORDER BY a.update_time DESC, a.id DESC
    </select>

    <select id="selectPendingByIds" resultMap="ImageAuditRecordMap">
        SELECT id, applicant_user_id, image_id, apply_reason, status,
               reviewer_user_id, reject_reason, create_time, review_time, update_time
        FROM biz_image_audit_record
        WHERE status = 1
          AND id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

    <select id="selectPendingByImageId" resultMap="ImageAuditRecordMap">
        SELECT id, applicant_user_id, image_id, apply_reason, status,
               reviewer_user_id, reject_reason, create_time, review_time, update_time
        FROM biz_image_audit_record
        WHERE status = 1
          AND image_id = #{imageId}
        LIMIT 1
    </select>

    <update id="batchReviewByIds">
        UPDATE biz_image_audit_record
        SET status = #{status},
            reviewer_user_id = #{reviewerUserId},
            reject_reason = #{rejectReason},
            review_time = NOW(),
            update_time = NOW()
        WHERE status = 1
          AND id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>

</mapper>
