package com.jacolp.module.note.api.model;

/**
 * Business outcome of an audit review. Persistence codes are intentionally not part of the API.
 */
public enum AuditDecision {
    APPROVED,
    REJECTED
}
