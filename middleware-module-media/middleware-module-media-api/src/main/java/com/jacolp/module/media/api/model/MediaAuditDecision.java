package com.jacolp.module.media.api.model;

/**
 * Business outcome of a media audit review. Persistence codes are intentionally not part of the API.
 */
public enum MediaAuditDecision {
    APPROVED,
    REJECTED
}
